// Use this to control the hashrouter's history such as programmatic navigation without reloading thr single page app

import { createBrowserHistory } from "history";
export default createBrowserHistory();
